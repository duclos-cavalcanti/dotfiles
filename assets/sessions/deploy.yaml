session_name: deploy
start_directory: "/home/duclos/.deploy/"
windows:
  - window_name: readme
    panes:
      - nvim README.md
  - window_name: src-bash
    layout: even-horizontal
    panes:
      - nvim ubuntu/install.sh
      - cd ubuntu && ls -l
