#!/bin/sh
# ram-info — memory in-use, formatted e.g. " 51%" (3-wide, % included).
# memory_pressure reports free %; in-use is the complement.
memory_pressure 2>/dev/null | awk -F': ' '/free percentage/ {gsub(/%/,"",$2); printf "%3d%%\n", 100-$2}'
