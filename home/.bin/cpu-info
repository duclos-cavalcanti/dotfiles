#!/bin/sh
# cpu-info — aggregate CPU usage, formatted e.g. "  7%" (3-wide, % included).
# Sum of per-process %cpu / online core count (100 = all cores saturated).
ps -A -o %cpu= | awk -v n="$(getconf _NPROCESSORS_ONLN)" '{s+=$1} END {printf "%3d%%\n", s/n}'
